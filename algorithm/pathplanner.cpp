#include "pathplanner.h"

PathPlanner::PathPlanner()
{
    // test
    path.push_back(Point(200, 0));
    path.push_back(Point(-200, 0));
}

void PathPlanner::plan()
{
    while(true){

    }
}

bool PathPlanner::hasArrived(Point target)
{
    return true;
}
